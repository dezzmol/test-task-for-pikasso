import React from 'react';
import {PostsList} from "entities/PostsList";

const MainPage = () => {
    return (
        <main>
            <PostsList />
        </main>
    );
};

export default MainPage;